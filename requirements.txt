# Task Tracker — Modules 1–3 baseline dependencies
# Stack is limited to what the module prompt libraries define:
# FastAPI + Uvicorn (API + server), Pydantic v2 (validation),
# python-dotenv (env loading), pytest + httpx (test suite / TestClient).
#
# Version pins below are lower bounds. After `pip install -r requirements.txt`,
# run `pip freeze` to record the exact versions resolved in your environment.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-dotenv>=1.0
pytest>=8
httpx>=0.27
